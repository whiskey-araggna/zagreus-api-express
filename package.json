{
  "name": "zagreus-api-express",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "NODE_ENV=dev DOTENV_CONFIG_PATH=./environment/.env.dev nodemon -r dotenv/config server.js",
    "start": "node -r dotenv/config server.js",
    "test:env": "NODE_ENV=dev DOTENV_CONFIG_PATH=./environment/.env.dev node -r dotenv/config src/config/env.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
